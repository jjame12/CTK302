let state =0 ;


function setup() {
  createCanvas(500, 500);
}

function draw() {

  switch(state){
    case 0:
    text(
      "shhhh!!!!",width/2, height/2) ;
      break ;
      case 1:
      text("QUIET DOWN!!!", width/2, height/2);
break ;
}
}
